import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-for-projects',
  standalone: true,
  imports: [RouterModule],
  templateUrl: './for-projects.html',
  styleUrl: './for-projects.scss'
})
export class ForProjects {}
